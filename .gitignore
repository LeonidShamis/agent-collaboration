# Materialized agent skills — installed/regenerable from skills-lock.json
# (pinned by hash from mattpocock/skills). Treated as dependencies, not source.
/.claude/skills/
/.agents/skills/

# Machine-local agent settings — never commit (may hold secrets/overrides)
*.local.json

# Node / bun
node_modules/

# Runtime Message Store (collaboration DB) and any stray SQLite files
.collab/
*.db
*.db-wal
*.db-shm

# OS / editor cruft
.DS_Store
*.swp
